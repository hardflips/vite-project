export enum RoutesEnum {
	LOGIN = '/',
	CARDS = '/u/cards',
	TABLE = '/u/table'
}

export enum RoutesNameEnum {
	NOT_FOUND = '404 - Página não encontrada',
	LOGIN = 'Login',
	CARDS = 'Cards',
	TABLE = 'Tabela'
}